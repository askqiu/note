 1  cd ../mylog
    2  cd d
    3  cd aaaaaaaa/git/gitmyblog
    4  cd aaaaaaaa/git/git/myblog
    5  npm install -g hexo-cli
    6  npm install -g hexo-cli
    7  hexo
    8  hexo -v
    9  hexo init
   10  hexo g
   11  hexo s
   12  cd ~/,ssh
   13  cd ~/.ssh
   14  git init
   15  cd ~/.ssh
   16  git status
   17  git  init
   18  hexo s
   19  hexo s
   20  hexo init blog
   21  hexo init
   22  hexo init
   23  npm install
   24  hexo g
   25  rm -rf node_modules
   26  rm -rf node_modules
   27  npm install --force
   28  npm cache clean --force
   29  npm cache clean
   30  npm init
   31  npm init
   32  hexo g
   33  cd ~/.ssh
   34  ssh -T git@github.com
   35  git
   36  git init
   37  git add .
   38  git commit -m "Initial commit"
   39  git config --global user.name "shadovv"
   40  git config --global user.email "1537276240@qq.com"
   41  git commit -m "Initial commit"
   42  git config --global user.name "askqiu"
   43  git commit -m "Initial commit"
   44  git remote add origin https://github.com/askqiu/note.git
   45  git add .
   46  git commit -m "Initial commit"
   47  git branch -M main  # 如果你的默认分支是 main
   48  git push -u origin main
   49  git remote -v
   50  git status
   51  git add .obsidian/plugins/yet-another-obsidian-synchronizer/
   52  git push origin main  # 如果你的默认分支是 main
   53  ssh-keygen -t ed25519 -C "1537276240@qq.com"
   54  git remote set-url origin git@github.com:askqiu/note.git
   55  ssh -T git@github.com
   56  git add .
   57  git commit -m "Add Obsidian synchronizer plugin"
   58  git push origin main
   59  npm install --force
   60  hexo g
   61  git remote -v
   62  ssh-add -1
   63  ssh-keygen -t rsa -b 4096 -C "1537276240@qq.com"
   64  ssh-keygen -t rsa -b 4096 -C "1537276240@qq.com"
   65  eval "$(ssh-agent -s)"
   66  ssh-add ~/.ssh/id_rsa
   67  cat ~/.ssh/id_rsa.pub | pbcopy
   68  cat ~/.ssh/id_rsa.pub
   69  ssh -T git@github.com
   70  git remote -v
   71  cd E:\BaiduSyncdisk\noteku\sec~
   72  cd E:\BaiduSyncdisk\noteku\sec
   73  e:
   74  E:
   75  cd "E:\BaiduSyncdisk\noteku\sec"
   76  git init
   77  git remote add origin git@github.com:askqiu/note.git
   78  npm install -g hexo-cli
   79  hpm -v
   80  npm -v
   81  npm install -g hexo-cli
   82  hexo init
   83  hexo init
   84  rm -rf node_modules && npm install --force
   85  npm install -g hexo-cli
   86  hexo init
   87  hexo g
   88  hexo g
   89  npm install -g hexo-cli
   90  hexo init
   91  hexo init
   92  hexo init
   93  hexo init
   94  hexo init
   95  npm config set registry https://registry.npmmirror.com
   96  hexo init
   97  hexo init
   98  hexo g
   99  npm install --force
  100  hexo g
  101  hexo s
  102  git clone -b master https://github.com/jerryc127/hexo-theme-butterfly.git themes/butterfly
  103  npm install hexo-renderer-pug hexo-renderer-stylus --save
  104  hexo g
  105  hexo s
  106  ssh
  107  ssh -T git@github.com
  108  npm install hexo-deployer-git --save
  109  hexo g
  110  hexo d
  111  hexo cl
  112  hexo g
  113  hexo d
  114  history
